// tailwind.config.js
export default {
  // Note: darkMode is configured in CSS for Tailwind v4
  content: ["./index.html", "./src/**/*.{js,jsx}"], // files to scan for Tailwind classes
  theme: {
    extend: {}, // extend default theme if needed
  },
  plugins: [],
};
